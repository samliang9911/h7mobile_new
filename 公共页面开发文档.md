# 公共页面（Unified Page）开发文档

---

## 1. 项目现状分析

### 1.1 现有代码结构

```
pages/subPackages/autopage/
├── index.vue                    # 动态表单主页面（现有）
├── components/
│   ├── modules/                 # 布局模块（现有）
│   │   ├── grid/               # 网格布局
│   │   ├── form/               # 表单模块
│   │   ├── row/                # 行组件
│   │   ├── column/             # 列组件
│   │   └── tabs/               # 标签页
│   ├── control/modules/         # 表单控件（现有）
│   │   ├── checkbox.vue
│   │   ├── chooseFrame.vue
│   │   ├── choosePage.vue
│   │   ├── date.vue
│   │   ├── input.vue
│   │   ├── radio.vue
│   │   ├── select.vue
│   │   └── textarea.vue
│   └── view/                   # 视图组件（现有）
│       ├── layout.vue
│       ├── render.vue
│       └── tabs.vue
└── control/                    # 控件导出（现有）
```

### 1.2 现有功能特点

| 模块 | 功能 | 状态 |
|------|------|------|
| **动态表单** | 通过配置动态渲染表单 | ✅ 已实现 |
| **表单控件** | 支持8种基础控件 | ✅ 已实现 |
| **布局系统** | grid/row/column布局 | ✅ 已实现 |
| **标签切换** | 表单/流程切换 | ✅ 已实现 |
| **表格功能** | 动态表格渲染 | ❌ 未实现 |
| **选择页面** | 弹窗选择人员/部门 | ✅ 已实现 |

### 1.3 现有入口逻辑

`index.vue` 核心流程：
1. 通过 URL 参数 `Dev_PageConfigOID` 获取页面配置
2. 调用 API 获取表单字段配置
3. 通过 `render` 组件动态渲染表单
4. 支持表单/流程标签切换

---

## 2. 开发目标

### 2.1 目标概述

打造一个**统一的页面组件**，整合以下核心功能：

| 功能模块 | 描述 |
|----------|------|
| **表单模块** | 支持多种字段类型的动态表单 |
| **表格模块** | 支持增删改查的动态表格 |
| **选择页面** | 人员/部门选择弹窗 |
| **配置驱动** | 通过 JSON 配置动态渲染 |

### 2.2 设计原则

1. **配置化**：通过 JSON 配置驱动页面渲染
2. **通用性**：支持多种业务场景
3. **可扩展**：易于添加新字段类型和功能
4. **复用性**：复用现有控件组件

---

## 3. 架构设计

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    Unified Page 入口                         │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │   NavBar    │  │    Tabs     │  │      内容区         │  │
│  └─────────────┘  └─────────────┘  │  ┌───────────────┐   │  │
│                                    │  │    Form       │   │  │
│                                    │  │  (表单模式)    │   │  │
│                                    │  └───────────────┘   │  │
│                                    │  ┌───────────────┐   │  │
│                                    │  │    Table      │   │  │
│                                    │  │  (表格模式)    │   │  │
│                                    │  └───────────────┘   │  │
│                                    └─────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│                    弹窗层 (Popups)                          │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐   │
│  │ ChoosePopup   │  │ DatePicker    │  │   其他弹窗    │   │
│  └───────────────┘  └───────────────┘  └───────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 组件分层

| 层级 | 组件 | 职责 |
|------|------|------|
| **页面层** | `unified-page.vue` | 页面入口，整合所有组件 |
| **布局层** | `NavBar.vue`, `Tabs.vue` | 页面导航和标签切换 |
| **内容层** | `Form.vue`, `Table.vue` | 表单和表格核心组件 |
| **字段层** | `FormField.vue` | 单字段渲染，适配多种类型 |
| **弹窗层** | `ChoosePopup.vue`, `DatePicker.vue` | 选择弹窗组件 |
| **状态层** | `unifiedPage.ts` (Pinia) | 全局状态管理 |

---

## 4. 核心数据结构

### 4.1 页面配置 (`PageConfig`)

```typescript
interface PageConfig {
  title: string                    // 页面标题
  mode: 'form' | 'table' | 'mixed' // 显示模式
  showSubmit?: boolean             // 是否显示提交按钮
  submitText?: string              // 提交按钮文字
  
  navigation?: NavigationConfig    // 导航栏配置
  form?: FormConfig                // 表单配置
  table?: TableConfig              // 表格配置
  data?: PageData                  // 初始数据
  events?: PageEvents              // 事件回调
}
```

### 4.2 表单字段配置 (`FieldConfig`)

```typescript
interface FieldConfig {
  guid: string                    // 唯一标识
  field: string                   // 字段名
  label: string                   // 显示标签
  type: FieldType                 // 字段类型
  required?: boolean              // 是否必填
  disabled?: boolean | string | Function  // 是否禁用（支持条件）
  visible?: boolean | string | Function   // 是否显示（支持条件）
  placeholder?: string            // 占位提示
  options?: FieldOption[]         // 选项列表（select/radio/checkbox）
  rules?: FieldValidation[]       // 验证规则
  props?: Record<string, any>     // 额外属性
}
```

### 4.3 支持的字段类型 (`FieldType`)

| 类型 | 说明 | 对应控件 |
|------|------|----------|
| `input` | 单行输入 | `input.vue` |
| `textarea` | 多行文本 | `textarea.vue` |
| `number` | 数字输入 | `input.vue` |
| `password` | 密码输入 | `input.vue` |
| `select` | 下拉选择 | `select.vue` |
| `radio` | 单选组 | `radio.vue` |
| `checkbox` | 多选组 | `checkbox.vue` |
| `switch` | 开关 | uview-switch |
| `date` | 日期选择 | `date.vue` |
| `time` | 时间选择 | `date.vue` |
| `datetime` | 日期时间 | `date.vue` |
| `daterange` | 日期范围 | 自定义 |
| `upload` | 文件上传 | uview-upload |
| `choosePage` | 选择页面 | `choosePage.vue` |
| `chooseUser` | 选择人员 | 选人页面 |
| `chooseDept` | 选择部门 | 选人页面 |

### 4.4 表格配置 (`TableConfig`)

```typescript
interface TableConfig {
  columns: TableColumn[]          // 列配置
  toolbar?: TableToolbar          // 工具栏
  rowActions?: RowAction[]        // 行操作按钮
  draggable?: boolean             // 是否可拖拽
  emptyText?: string              // 空状态提示
  maxRows?: number                // 最大行数
}

interface TableColumn {
  field: string                   // 字段名
  label: string                   // 显示标签
  width?: string | number         // 列宽
  align?: 'left' | 'center' | 'right'
  editable?: boolean              // 是否可编辑
  editorType?: FieldType          // 编辑器类型
  formatter?: Function            // 格式化函数
}
```

---

## 5. 开发计划

### 5.1 Phase 1: 基础架构（已完成）

| 任务 | 文件 | 状态 |
|------|------|------|
| 创建类型定义 | `unified-page.types.ts` | ✅ |
| 创建导航栏组件 | `NavBar.vue` | ✅ |
| 创建标签页组件 | `Tabs.vue` | ✅ |
| 创建表单容器 | `Form.vue` | ✅ |
| 创建字段组件 | `FormField.vue` | ✅ |
| 创建表格组件 | `Table.vue` | ✅ |
| 创建选择弹窗 | `ChoosePopup.vue` | ✅ |
| 创建日期弹窗 | `DatePicker.vue` | ✅ |
| 创建状态管理 | `store/unifiedPage.ts` | ✅ |
| 注册路由 | `pages.json` | ✅ |

### 5.2 Phase 2: 功能完善（待开发）

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 字段条件渲染 | 支持 `visible/disabled` 条件表达式 | P0 |
| 表单验证 | 实现完整的验证规则引擎 | P0 |
| 表格编辑 | 支持行内编辑和数据校验 | P0 |
| 选人集成 | 对接现有选人页面 | P0 |
| 文件上传 | 完善上传组件 | P1 |
| 日期范围 | 实现日期范围选择 | P1 |

### 5.3 Phase 3: 优化与测试（待开发）

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 性能优化 | 虚拟滚动、懒加载 | P1 |
| 样式优化 | 统一设计风格 | P2 |
| 单元测试 | 核心组件测试 | P2 |
| 文档完善 | API 文档和使用示例 | P2 |

---

## 6. 数据流向

### 6.1 配置加载流程

```
URL参数 → 解析配置 → 初始化数据 → 渲染组件
    ↓           ↓           ↓
 config      formData     Form/Table
              tableData      ↓
                           用户操作
                             ↓
                      更新formData/tableData
                             ↓
                          提交验证
                             ↓
                         提交数据
```

### 6.2 状态管理架构

```
Pinia Store (unifiedPage)
├── config: PageConfig       # 页面配置
├── formData: Record         # 表单数据
├── tableData: TableRow[]    # 表格数据
├── validation: Record       # 验证状态
├── currentTab: 'form|table' # 当前标签
└── submitting: boolean      # 提交状态

Actions:
├── initPage(config)         # 初始化页面
├── updateFormField(field, value) # 更新字段
├── addTableRow(row)         # 添加表格行
├── deleteTableRow(index)    # 删除表格行
├── submitForm()             # 提交表单
└── validateForm()           # 验证表单
```

---

## 7. API 调用方式

### 7.1 方式一：直接访问

```javascript
uni.navigateTo({
  url: '/pages/subPackages/autopage/unified-page'
})
```

### 7.2 方式二：传递配置参数

```javascript
const config = {
  title: '员工信息录入',
  mode: 'form',
  showSubmit: true,
  form: {
    fields: [
      {
        guid: '1',
        field: 'name',
        label: '姓名',
        type: 'input',
        required: true,
        placeholder: '请输入姓名'
      },
      {
        guid: '2',
        field: 'gender',
        label: '性别',
        type: 'radio',
        options: [
          { label: '男', value: 'male' },
          { label: '女', value: 'female' }
        ]
      }
    ]
  },
  data: {
    name: '张三',
    gender: 'male'
  },
  events: {
    onSubmitSuccess: (data) => {
      console.log('提交成功:', data)
    }
  }
}

const url = `/pages/subPackages/autopage/unified-page?config=${encodeURIComponent(JSON.stringify(config))}`
uni.navigateTo({ url })
```

### 7.3 方式三：混合模式（表单+表格）

```javascript
const config = {
  title: '采购申请',
  mode: 'mixed',
  showSubmit: true,
  form: {
    fields: [
      { guid: '1', field: 'applicant', label: '申请人', type: 'chooseUser' },
      { guid: '2', field: 'department', label: '部门', type: 'chooseDept' },
      { guid: '3', field: 'applyDate', label: '申请日期', type: 'date' }
    ]
  },
  table: {
    columns: [
      { field: 'itemName', label: '物品名称', editable: true },
      { field: 'quantity', label: '数量', editable: true, editorType: 'number' },
      { field: 'unit', label: '单位' },
      { field: 'price', label: '单价', editorType: 'number' }
    ],
    toolbar: {
      show: true,
      buttons: [{ key: 'add', label: '添加行', type: 'success' }]
    },
    rowActions: [
      { key: 'delete', label: '删除', type: 'danger', action: 'delete' }
    ]
  }
}
```

---

## 8. 与现有系统集成

### 8.1 复用现有控件

统一页面组件将复用 `control/modules/` 下的现有控件：

```typescript
// FormField.vue 内部逻辑
import InputControl from '../control/modules/input.vue'
import SelectControl from '../control/modules/select.vue'
// ... 其他控件

const renderControl = (field) => {
  switch (field.type) {
    case 'input':
      return h(InputControl, { ...field.props })
    case 'select':
      return h(SelectControl, { ...field.props })
    // ... 其他类型
  }
}
```

### 8.2 对接选人页面

```typescript
// 在 choosePage 类型字段点击时
const handleChooseUser = () => {
  uni.navigateTo({
    url: '/pages/subPackages/publicform/publicChoicePerson',
    events: {
      ChoicePerson: (result) => {
        // 处理选择结果
        formData.value[field.field] = result
      }
    }
  })
}
```

---

## 9. 注意事项

### 9.1 性能考虑

1. **大数据量表格**：考虑虚拟滚动
2. **复杂表单**：按需加载和条件渲染
3. **重复渲染**：使用 `v-show` 替代 `v-if` 优化切换性能

### 9.2 兼容性

1. **小程序端**：注意 API 差异
2. **H5 端**：注意样式兼容性
3. **App 端**：注意原生组件适配

### 9.3 安全考虑

1. **输入验证**：服务端二次验证
2. **XSS 防护**：对用户输入进行转义
3. **敏感数据**：密码字段加密传输

---

## 10. 下一步计划

### 10.1 需要对齐的问题

| 问题 | 说明 |
|------|------|
| 字段类型优先级 | 是否优先使用现有 control 控件还是重新封装？ |
| 表格编辑方式 | 行内编辑还是弹窗编辑？ |
| 数据提交方式 | 通过 API 还是事件回调？ |
| 验证规则 | 使用内置验证还是自定义验证函数？ |
| 状态管理 | 是否必须使用 Pinia？ |

### 10.2 建议开发顺序

1. **确认架构方案** → 对齐设计文档
2. **完善类型定义** → 补充缺失类型
3. **实现核心组件** → FormField、Table
4. **集成现有控件** → 复用 control 模块
5. **实现弹窗组件** → ChoosePopup、DatePicker
6. **完善验证逻辑** → 表单验证引擎
7. **对接选人页面** → 集成公共选人
8. **编写测试用例** → 单元测试
9. **优化样式** → 统一设计风格
